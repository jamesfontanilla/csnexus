"""FastAPI router for announcement endpoints.

Phase 2 will add learner-facing routes:
- GET /v1/announcements — list active announcements for the current user
- POST /v1/announcements/{id}:dismiss — dismiss an announcement

For now, admin creation is handled via POST /v1/admin/announcements
in the admin router (Task 17.6).
"""
